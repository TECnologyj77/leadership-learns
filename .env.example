# Public project token from PostHog Project Settings (never a personal API key).
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://us.i.posthog.com
# Defaults to enabled only on the production domain. Set true to test locally
# or on previews; those events are labeled development/preview.
VITE_POSTHOG_ENABLED=
